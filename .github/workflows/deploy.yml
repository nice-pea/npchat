# Стянуть образ и запустить docker-контейнер в рабочей среде
name: CD Deploy

on:
  workflow_dispatch:
    inputs:
      tag:
        type: string
        description: 'тег docker-образа для развертывания'

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Проверить существование docker-образа
        # Written with gigacode
        id: check-image
        run: |
          TAG=${{ inputs.tag || github.ref_name }}
          IMAGE_NAME=${{ vars.DOCKER_IMAGE_NAME }}

          echo "Проверяю существование образа $IMAGE_NAME:$TAG..."

          # Авторизоваться в registry
          docker login -u "${{ secrets.DOCKER_USERNAME }}" -p "${{ secrets.DOCKER_PASSWORD }}"

          # Попробуем получить информацию о манифесте образа
          if ! docker manifest inspect "$IMAGE_NAME:$TAG" > /dev/null 2>&1; then
            echo "❌ Образ $IMAGE_NAME:$TAG не существует в registry."
            exit 1
          else
            echo "✅ Образ $IMAGE_NAME:$TAG существует."
          fi

      - name: Развернуть новый образ
        uses: appleboy/ssh-action@v1.2.2
        # continue-on-error: true
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            set -e
            NEW_IMAGE=${{ inputs.tag || github.ref_name }}
            CONTAINER_NAME=npc.npchat
            PREV_IMAGE=$(docker inspect --format='{{.Config.Image}}' $CONTAINER_NAME 2>/dev/null | sed 's/^.*://')
            
            echo "Развертывание $CONTAINER_NAME:$NEW_IMAGE..."
            
            # Останавливаем текущий контейнер
            docker stop $CONTAINER_NAME || true
            docker rm $CONTAINER_NAME || true
            
            # Загрузить переменные окружения, чтобы были доступны в docker-compose
            set -o allexport
            source /opt/npchat/.env.production
            set +o allexport
            
            # Название образа
            export IMAGE_NAME=${{ vars.DOCKER_IMAGE_NAME }}
            
            # Запускаем новый
            if IMAGE_TAG=$NEW_IMAGE docker compose -f /opt/npchat/infra/npchat/docker-compose.yaml up --wait npc.npchat; then
              echo "✅ Успешно развернут $CONTAINER_NAME:$NEW_IMAGE"
              exit 0
            fi
            
            echo "❌ Развернуть $CONTAINER_NAME:$NEW_IMAGE не удалось"
            
            # Пытаемся восстановить предыдущий контейнер
            if [ -n "$PREV_IMAGE" ]; then
              echo "🔄 Откат к $CONTAINER_NAME:$PREV_IMAGE..."
              IMAGE_TAG=$PREV_IMAGE docker compose -f /opt/npchat/infra/npchat/docker-compose.yaml up --wait npc.npchat
              echo "🆗 Успешно развернута прошлая версия $CONTAINER_NAME:$NEW_IMAGE"
            fi
            
            exit 2